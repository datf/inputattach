# Automatically added by dh_installinit
if [ -x "/etc/init.d/joystick" ]; then
	update-rc.d joystick defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d joystick start || true
	else
		/etc/init.d/joystick start || true
	fi
fi
# End automatically added section
